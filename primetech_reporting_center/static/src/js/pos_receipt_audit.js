/** @odoo-module **/

// Compatibility shim: older asset attachments and overlay deployments may still
// reference this backend path. POS-only code lives in static/src/pos so this
// module must deliberately have no @point_of_sale dependency.
